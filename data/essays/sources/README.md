# Essay Source Files

Put original essay `.docx` or `.pdf` files here. After extraction, add the
cleaned website text as a sibling `.ts` entry in `data/essays/` and export it
from `data/essays/index.ts`.
