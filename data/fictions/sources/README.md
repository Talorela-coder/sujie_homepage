# Fiction Source Files

Put original fiction `.docx` or `.pdf` files here. After extraction, add the
cleaned website text as a sibling `.ts` entry in `data/fictions/` and export it
from `data/fictions/index.ts`.
