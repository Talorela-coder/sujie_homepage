# Letter Source Files

Put original letter `.docx` or `.pdf` files here. After extraction, add the
cleaned website text as a sibling `.ts` entry in `data/letters/` and export it
from `data/letters/index.ts`.
