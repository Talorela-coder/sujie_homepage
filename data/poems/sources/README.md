# Poem Source Files

Put original poem `.docx` or `.pdf` files here. After extraction, add the cleaned
website text as a sibling `.ts` entry in `data/poems/` and export it from
`data/poems/index.ts`.
