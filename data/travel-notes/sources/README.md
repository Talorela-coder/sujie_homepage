# Travel Note Source Files

Put original travel note `.docx` or `.pdf` files here. After extraction, add the
cleaned website text as a sibling `.ts` entry in `data/travel-notes/` and export
it from `data/travel-notes/index.ts`.
